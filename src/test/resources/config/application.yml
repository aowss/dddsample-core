spring:
    dataSource:
        url: jdbc:hsqldb:mem:dddsample_test
    sql:
        init:
            mode: always
    main:
        allow-bean-definition-overriding: true
server:
    error:
        include-message: always